

! Dans touchesEnded a la fin
// Create Sprite 2 ( Projectile)
let projectile = SKSpriteNode(imageNamed: "projectile")
projectile.position = player.position
projectile.physicsBody = SKPhysicsBody(circleOfRadius: projectile.size.width/2)
projectile.physicsBody?.isDynamic = true
projectile.physicsBody?.categoryBitMask = PhysicsCategory.projectile
projectile.physicsBody?.contactTestBitMask = PhysicsCategory.monster
projectile.physicsBody?.collisionBitMask = PhysicsCategory.none
// pour utiliser un algo ameliorer de detection de collision
projectile.physicsBody?.usesPreciseCollisionDetection = true
addChild(projectile)

// gere le pop de monstre a l'infini
/*run(SKAction.repeatForever(
SKAction.sequence([
// lance addMonster
SKAction.run(addMonster),
// att 5sec
SKAction.wait(forDuration: 5.0)
])
))*/


//let backgroundMusic = SKAudioNode(fileNamed: "background-music-aac.caf")
//backgroundMusic.autoplayLooped = true
//addChild(backgroundMusic)


// run le pew pew son
//run(SKAction.playSoundFileNamed("pew-pew-lei.caf", waitForCompletion: false))
// get la position du toucher 


TODO :
  Changer les textures
  Mettre en place la jauge d'energie qui tourne tout le temps
    // lancer une animation forever qui
      // si value < 100
          incremente la value de energy bar
          lance la fonction qui re set la position de la barre (ou alors directement reset la position de la barre)

lance une fonction avec une valeur
    // 100 = on fait rien
    /
  Si startMode = true && joueur clique sur Maraudeur alors reduire la jauge d'energie

  Trouver un moyen de mettre des putin de valeurs gobale au projet
